services:
  - type: web
    name: vanlostandfound-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeployTrigger: commit

databases:
  - name: vanlostandfound-db
